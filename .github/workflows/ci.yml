name: ci (deprecated)

# ДЕАКТИВИРОВАНО: оставлено для истории. Не запускается автоматически.
on:
  workflow_call:

jobs:
  noop:
    name: Deprecated workflow (no-op)
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "This workflow is deprecated and intentionally does nothing."
